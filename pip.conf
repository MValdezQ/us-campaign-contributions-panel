# Project pip config — prefer public PyPI for this repository
# Use this file in CI or locally by setting PIP_CONFIG_FILE to the path below.
# To use: $env:PIP_CONFIG_FILE = "$PWD\.pip\pip.conf" (PowerShell, session)

[global]
index-url = https://pypi.org/simple
trusted-host = pypi.org
timeout = 60
